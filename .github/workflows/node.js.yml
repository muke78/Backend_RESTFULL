# .github/workflows/nodejs.yml

name: Node.js CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main", "dev"]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [22.x]
        # Agrega más versiones si deseas: [18.x, 20.x, 22.x]

    steps:
      - name: 🔄 Checkout del código
        uses: actions/checkout@v4

      - name: 🟢 Usar Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: 💿 Instalar pnpm
        run: npm install -g pnpm

      - name: 📦 Instalar dependencias
        run: pnpm install

      # 🔽 Descomenta este paso solo si necesitas levantar el servidor antes de correr tests
      # - name: 🚀 Iniciar aplicación en background (opcional)
      #   run: pnpm start &

      - name: ✅ Ejecutar pruebas
        run: pnpm test
